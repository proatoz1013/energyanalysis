import os

def get_public_holidays(year, api_key=None):
    """
    Disabled: Always returns an empty set. No API call is made.
    """
    return set()

def get_malaysia_public_holidays(year: int, api_key=None) -> set:
    """
    Disabled: Always returns an empty set. No API call is made.
    """
    return set()

# Example usage:
# holidays = get_malaysia_public_holidays(2025, api_key="your_api_key_here")
# is_peak = is_peak_rp4(datetime.datetime.now(), holidays)
